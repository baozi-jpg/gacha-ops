<Window x:Class="GachaOps.App.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="460" SizeToContent="Height" MaxHeight="650"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Border Margin="28" Padding="28" Background="{StaticResource PageBackground}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border x:Name="IconBadge" Width="56" Height="56" HorizontalAlignment="Center"
                    Background="{StaticResource PrimaryBrush}" CornerRadius="28">
                <TextBlock x:Name="IconGlyph" Text="i" HorizontalAlignment="Center"
                           VerticalAlignment="Center" Foreground="White" FontSize="25"
                           FontWeight="SemiBold" FontFamily="Segoe UI Variable Text, Segoe UI" />
            </Border>

            <TextBlock x:Name="DialogTitleText" Grid.Row="1" Margin="0,18,0,0"
                       HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"
                       FontSize="21" FontWeight="SemiBold"
                       Foreground="{StaticResource PrimaryTextBrush}" />

            <ScrollViewer x:Name="MessageScrollViewer" Grid.Row="2" Margin="0,14,0,0"
                          MaxHeight="320" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <TextBlock x:Name="DialogMessageText" TextWrapping="Wrap"
                           Foreground="{StaticResource PrimaryTextBrush}" />
            </ScrollViewer>

            <Button x:Name="ConfirmButton" Grid.Row="3" Content="确定" Width="112"
                    Margin="0,24,0,0" HorizontalAlignment="Center" IsDefault="True"
                    Click="ConfirmButton_Click" />
        </Grid>
    </Border>
</Window>
